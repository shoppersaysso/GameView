<% if games.empty? %>
<%= "You don't have any games to display. To get started, add a video game you own. You can set the status to new, playing, or beat. You can also review it. The more reviews you make, the higher your GameView Level will be!" %>
<% else %>
<% games.each do |game|%>
  <p><strong><%= link_to game.title, game %></strong></p>
  <br>
    <div id="details-<%= game.id %>">
      <p><em><%= truncate game.details %></em></p>
    </div>
    <br>
    <button id="<%= game.id %>" class="js-more" data-id="<% game.id %>">Show Details...</button>
  <% end %>
<% end %>
