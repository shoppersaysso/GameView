<p id="notice"><%= notice %></p>

<h1><%= @review.title %></h1>
<h2><%= @game.title %></h2>
<h3><%= @review.content %></h3>

<%= link_to 'Edit', edit_game_review_path(@review.game, @review) %> |
<%= link_to 'Delete', delete_review_path(@review.game, @review), method: :delete, data: { confirm: 'Are you sure?' } %></p> |
<%= link_to 'Back', home_path %>
